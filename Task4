
clear
clc

interesttype=input('1.SI or 2.CI:');

if interesttype == 1;
    disp('SI:')
    principal = input('Enter the Original amount borrowed or deposited:');
    rate = input('Enter R:');
    time = input('Enter T:');
    if rate < 1;
        interest = principal * rate * time;
        disp('The interest was equal to:')
        disp(interest)
    else rate > 1;
        interest = principal * (rate/100)*time;
        disp('Interest:')
        disp(interest)
    end
elseif interesttype ==2 ;
    disp('CI:')
    principal = input('Enter P:');
    rate = input('Enter R:');
    time = input('Enter T:');
  
    if rate < 1;
        interest = principal *((1 + rate)^time);
        disp('Interest:')
        disp(interest)
    else rate > 1;
        interest = principal * ((1 + rate/100)^time);
        disp('The interest was equal to:')
        disp(interest)
    end
end 
